/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { HrTime } from '@opentelemetry/api';
import { ReadableSpan, SpanExporter, SpanProcessor } from '@opentelemetry/sdk-trace-base';
/**
 * Converts HrTime to nanoseconds timestamp
 *
 * @param hrTime The HrTime array [seconds, nanoseconds]
 * @returns Time in nanoseconds as a number
 */
export declare function hrTimeToNanoseconds(hrTime: HrTime): number;
export declare class ApiServerSpanExporter implements SpanExporter {
    private traceDict;
    constructor(traceDict: Record<string, Record<string, unknown>>);
    export(spans: ReadableSpan[], resultCallback: (result: {
        code: number;
    }) => void): void;
    forceFlush(): Promise<void>;
    shutdown(): Promise<void>;
}
export declare class InMemoryExporter implements SpanExporter {
    private spans;
    private traceDict;
    constructor(traceDict: Record<string, string[]>);
    export(spans: ReadableSpan[], resultCallback: (result: {
        code: number;
    }) => void): void;
    forceFlush(): Promise<void>;
    shutdown(): Promise<void>;
    getFinishedSpans(sessionId: string): ReadableSpan[];
    clear(): void;
}
export declare function setupTelemetry(otelToCloud?: boolean, internalExporters?: SpanProcessor[]): Promise<void>;
