
const importMetaUrl = /* @__PURE__ */ require("node:url").pathToFileURL(__filename).toString();
const importMetaFilename = /* @__PURE__ */ __filename;
const importMetaDirname = /* @__PURE__ */ __dirname;
const importMetaResolve = /* @__PURE__ */ require.resolve;
"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var yaml_test_loader_exports = {};
__export(yaml_test_loader_exports, {
  batchLoadYamlTestDefs: () => batchLoadYamlTestDefs
});
module.exports = __toCommonJS(yaml_test_loader_exports);
var import_camelcase_keys = __toESM(require("camelcase-keys"), 1);
var import_fast_glob = __toESM(require("fast-glob"), 1);
var import_js_yaml = __toESM(require("js-yaml"), 1);
var fs = __toESM(require("node:fs/promises"), 1);
var path = __toESM(require("node:path"), 1);
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
async function batchLoadYamlTestDefs(directory) {
  const files = import_fast_glob.default.stream("**/spec.{yaml,yml}", {
    cwd: directory,
    absolute: true
  });
  const tests = /* @__PURE__ */ new Map();
  for await (const file of files) {
    const normalizedFile = file.replaceAll("\\", "/");
    const baseDir = path.posix.dirname(normalizedFile);
    const specFile = path.posix.join(baseDir, "spec.yaml");
    const filePath = specFile;
    const content = await fs.readFile(filePath, "utf-8");
    const parsedSpec = import_js_yaml.default.load(content);
    if (typeof parsedSpec !== "object" || parsedSpec === null) {
      throw new Error("Spec file must be a YAML mapping");
    }
    const testSpec = (0, import_camelcase_keys.default)(parsedSpec, {
      deep: true
    });
    const sessionFile = path.posix.join(baseDir, "generated-session.yaml");
    const sessionContent = await fs.readFile(sessionFile, "utf-8");
    const parsedSession = import_js_yaml.default.load(sessionContent);
    if (typeof parsedSession !== "object" || parsedSession === null) {
      throw new Error("Session file must be a YAML mapping");
    }
    const session = (0, import_camelcase_keys.default)(parsedSession, {
      deep: true
    });
    const recordingsFile = path.posix.join(
      baseDir,
      "generated-recordings.yaml"
    );
    const recordingsContent = await fs.readFile(recordingsFile, "utf-8");
    const parsedRecordings = import_js_yaml.default.load(recordingsContent);
    if (typeof parsedRecordings !== "object" || parsedRecordings === null) {
      throw new Error("Recording file must be a YAML mapping");
    }
    const recordings = (0, import_camelcase_keys.default)(parsedRecordings, {
      deep: true
    });
    const normalizedDir = directory.replaceAll("\\", "/");
    const relativePath = path.posix.relative(normalizedDir, baseDir);
    const parsedPath = path.posix.parse(relativePath);
    const name = path.posix.join(parsedPath.dir, parsedPath.name);
    tests.set(name, {
      name,
      spec: testSpec,
      session,
      recordings
    });
    console.log("loaded test", name, "from", baseDir);
  }
  return tests;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  batchLoadYamlTestDefs
});
