
const importMetaUrl = /* @__PURE__ */ require("node:url").pathToFileURL(__filename).toString();
const importMetaFilename = /* @__PURE__ */ __filename;
const importMetaDirname = /* @__PURE__ */ __dirname;
const importMetaResolve = /* @__PURE__ */ require.resolve;
"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var run_integration_tests_exports = {};
__export(run_integration_tests_exports, {
  runIntegrationTests: () => runIntegrationTests
});
module.exports = __toCommonJS(run_integration_tests_exports);
var import_conformance_integrations = require("../conformance/conformance_integrations.js");
var import_yaml_agent_loader = require("../conformance/yaml_agent_loader.js");
var import_yaml_test_loader = require("../conformance/yaml_test_loader.js");
var import_agent_registry = require("./agent_registry.js");
var import_integration_registry = require("./integration_registry.js");
var import_test_runner = require("./test_runner.js");
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
async function runIntegrationTests({
  agentsDir,
  testsDir,
  forceRunAll
}) {
  console.log(`Loading agents from ${agentsDir}`);
  const agentConfigs = await (0, import_yaml_agent_loader.batchLoadYamlAgentConfig)(agentsDir);
  console.log(agentConfigs.size, "agents found");
  console.log("Registering conformance integrations.");
  const registry = new import_integration_registry.IntegrationRegistry();
  (0, import_conformance_integrations.registerConformanceIntegrations)(registry);
  console.log(registry.summary());
  console.log("Registering agents.");
  const agentRegistry = new import_agent_registry.AgentRegistry(registry);
  for (const [name, agentConfig] of agentConfigs) {
    agentRegistry.registerAgentConfig(name, agentConfig);
  }
  console.log(agentRegistry.summary());
  console.log(`Loading tests from ${testsDir}`);
  const testSpecs = await (0, import_yaml_test_loader.batchLoadYamlTestDefs)(testsDir);
  console.log(testSpecs.size, "tests found.");
  console.log("Running tests.");
  const successfulTests = [];
  const skippedTests = [];
  const failedTests = [];
  const testRunner = new import_test_runner.TestRunner(agentRegistry);
  for (const [name, testInfo] of testSpecs) {
    console.log("\x1B[33mRunning test", name, "\x1B[0m\n");
    try {
      const skipped = await testRunner.run(testInfo, forceRunAll);
      if (skipped) {
        skippedTests.push(name);
        console.log("\n\x1B[33mTest skipped.\x1B[0m\n");
        continue;
      }
      successfulTests.push(name);
      console.log("\n\x1B[32mTest passed.\x1B[0m\n");
    } catch (_) {
      failedTests.push(name);
      console.error("\n\x1B[31mTest failed.\x1B[0m\n");
    }
  }
  console.log(
    `

${successfulTests.length} tests passed, ${skippedTests.length} tests skipped, ${failedTests.length} tests failed.`
  );
  console.log("Successfull tests:", successfulTests.join(", "));
  console.log("Skipped tests:", skippedTests.join(", "));
  console.log("Failed tests:", failedTests.join(", "));
  console.log("\n");
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  runIntegrationTests
});
