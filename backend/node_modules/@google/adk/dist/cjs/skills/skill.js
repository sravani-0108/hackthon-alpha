/**
  * @license
  * Copyright 2026 Google LLC
  * SPDX-License-Identifier: Apache-2.0
  */

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var skill_exports = {};
__export(skill_exports, {
  FrontmatterSchema: () => FrontmatterSchema,
  SNAKE_OR_KEBAB_NAME_PATTERN: () => SNAKE_OR_KEBAB_NAME_PATTERN
});
module.exports = __toCommonJS(skill_exports);
var import_zod = require("zod");
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
const SNAKE_OR_KEBAB_NAME_PATTERN = /^([a-z0-9]+(-[a-z0-9]+)*|[a-z0-9]+(_[a-z0-9]+)*)$/;
const FrontmatterSchema = import_zod.z.preprocess(
  (data) => {
    if (typeof data === "object" && data !== null) {
      const obj = data;
      if ("allowed-tools" in obj && !("allowedTools" in obj)) {
        return {
          ...obj,
          allowedTools: obj["allowed-tools"]
        };
      }
    }
    return data;
  },
  import_zod.z.object({
    name: import_zod.z.string().regex(SNAKE_OR_KEBAB_NAME_PATTERN, {
      message: "name must be lowercase kebab-case (a-z, 0-9, hyphens) or snake_case (a-z, 0-9, underscores), with no leading, trailing, or consecutive delimiters. Mixing hyphens and underscores is not allowed."
    }).max(64),
    description: import_zod.z.string().min(1).max(1024),
    license: import_zod.z.string().optional(),
    compatibility: import_zod.z.string().max(500).optional(),
    "allowed-tools": import_zod.z.string().optional(),
    metadata: import_zod.z.record(import_zod.z.string(), import_zod.z.any()).default({}).refine(
      (data) => {
        if ("adk_additional_tools" in data) {
          return Array.isArray(data.adk_additional_tools) && data.adk_additional_tools.every(
            (item) => typeof item === "string"
          );
        }
        return true;
      },
      {
        message: "adk_additional_tools must be a list of strings"
      }
    )
  }).loose()
);
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  FrontmatterSchema,
  SNAKE_OR_KEBAB_NAME_PATTERN
});
