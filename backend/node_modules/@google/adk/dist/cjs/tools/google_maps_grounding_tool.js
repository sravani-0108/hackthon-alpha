/**
  * @license
  * Copyright 2026 Google LLC
  * SPDX-License-Identifier: Apache-2.0
  */

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var google_maps_grounding_tool_exports = {};
__export(google_maps_grounding_tool_exports, {
  GOOGLE_MAPS_GROUNDING: () => GOOGLE_MAPS_GROUNDING,
  GoogleMapsGroundingTool: () => GoogleMapsGroundingTool,
  applyGoogleMapsGrounding: () => applyGoogleMapsGrounding
});
module.exports = __toCommonJS(google_maps_grounding_tool_exports);
var import_model_name = require("../utils/model_name.js");
var import_base_tool = require("./base_tool.js");
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
function applyGoogleMapsGrounding(llmRequest) {
  if (!llmRequest.model) {
    return;
  }
  const modelCheckDisabled = (0, import_model_name.isGeminiModelIdCheckDisabled)();
  llmRequest.config = llmRequest.config || {};
  llmRequest.config.tools = llmRequest.config.tools || [];
  if ((0, import_model_name.isGemini1Model)(llmRequest.model)) {
    throw new Error(
      "Google Maps grounding tool cannot be used with Gemini 1.x models."
    );
  }
  if ((0, import_model_name.isGeminiModel)(llmRequest.model) || modelCheckDisabled) {
    llmRequest.config.tools.push({
      googleMaps: {}
    });
    return;
  }
  throw new Error(
    `Google maps tool is not supported for model ${llmRequest.model}`
  );
}
class GoogleMapsGroundingTool extends import_base_tool.BaseTool {
  constructor() {
    super({ name: "google_maps", description: "Google Maps Grounding Tool" });
  }
  runAsync() {
    return Promise.resolve();
  }
  async processLlmRequest({
    llmRequest
  }) {
    applyGoogleMapsGrounding(llmRequest);
  }
}
const GOOGLE_MAPS_GROUNDING = new GoogleMapsGroundingTool();
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  GOOGLE_MAPS_GROUNDING,
  GoogleMapsGroundingTool,
  applyGoogleMapsGrounding
});
