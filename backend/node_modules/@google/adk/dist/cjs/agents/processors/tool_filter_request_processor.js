/**
  * @license
  * Copyright 2026 Google LLC
  * SPDX-License-Identifier: Apache-2.0
  */

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var tool_filter_request_processor_exports = {};
__export(tool_filter_request_processor_exports, {
  TOOL_FILTER_REQUEST_PROCESSOR: () => TOOL_FILTER_REQUEST_PROCESSOR,
  ToolFilterRequestProcessor: () => ToolFilterRequestProcessor
});
module.exports = __toCommonJS(tool_filter_request_processor_exports);
var import_context = require("../context.js");
var import_llm_agent = require("../llm_agent.js");
var import_readonly_context = require("../readonly_context.js");
var import_base_llm_processor = require("./base_llm_processor.js");
/**
 * @license
 * Copyright 2025 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
class ToolFilterRequestProcessor extends import_base_llm_processor.BaseLlmRequestProcessor {
  /** Filters the set of tools on the request based on plugins. */
  // eslint-disable-next-line require-yield
  async *runAsync(invocationContext, llmRequest) {
    const agent = invocationContext.agent;
    if (!(0, import_llm_agent.isLlmAgent)(agent)) {
      return;
    }
    const toolsList = await agent.canonicalTools(
      new import_readonly_context.ReadonlyContext(invocationContext)
    );
    if (toolsList.length === 0) {
      return;
    }
    const toolsDict = {};
    for (const tool of toolsList) {
      toolsDict[tool.name] = tool;
    }
    const callbackContext = new import_context.Context({ invocationContext });
    const filteredTools = await invocationContext.pluginManager.runBeforeToolSelection({
      callbackContext,
      tools: toolsDict
    });
    if (filteredTools !== void 0) {
      llmRequest.allowedTools = Object.keys(filteredTools);
    }
  }
}
const TOOL_FILTER_REQUEST_PROCESSOR = new ToolFilterRequestProcessor();
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  TOOL_FILTER_REQUEST_PROCESSOR,
  ToolFilterRequestProcessor
});
