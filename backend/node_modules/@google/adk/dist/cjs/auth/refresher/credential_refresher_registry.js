/**
  * @license
  * Copyright 2026 Google LLC
  * SPDX-License-Identifier: Apache-2.0
  */

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var credential_refresher_registry_exports = {};
__export(credential_refresher_registry_exports, {
  CredentialRefresherRegistry: () => CredentialRefresherRegistry
});
module.exports = __toCommonJS(credential_refresher_registry_exports);
var import_auth_credential = require("../auth_credential.js");
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
class CredentialRefresherRegistry {
  constructor() {
    this.refreshers = {
      [import_auth_credential.AuthCredentialTypes.API_KEY]: void 0,
      [import_auth_credential.AuthCredentialTypes.HTTP]: void 0,
      [import_auth_credential.AuthCredentialTypes.OAUTH2]: void 0,
      [import_auth_credential.AuthCredentialTypes.OPEN_ID_CONNECT]: void 0,
      [import_auth_credential.AuthCredentialTypes.SERVICE_ACCOUNT]: void 0
    };
  }
  /**
   * Register a refresher instance for a credential type.
   *
   * @param credentialType The credential type to register for.
   * @param refresherInstance The refresher instance to register.
   */
  register(credentialType, refresherInstance) {
    this.refreshers[credentialType] = refresherInstance;
  }
  /**
   * Get the refresher instance for a credential type.
   *
   * @param credentialType The credential type to get refresher for.
   * @returns The refresher instance if registered, undefined otherwise.
   */
  getRefresher(credentialType) {
    return this.refreshers[credentialType];
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  CredentialRefresherRegistry
});
