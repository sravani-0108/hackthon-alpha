/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { AuthCredential } from '../auth_credential.js';
import { AuthScheme, OAuthGrantType } from '../auth_schemes.js';
import { BaseCredentialExchanger, ExchangeResult } from '../exchanger/base_credential_exchanger.js';
/**
 * Exchanges OAuth2 credentials from authorization responses using standard fetch.
 */
export declare class OAuth2CredentialExchanger implements BaseCredentialExchanger {
    exchange({ authCredential, authScheme, }: {
        authCredential: AuthCredential;
        authScheme?: AuthScheme;
    }): Promise<ExchangeResult>;
}
export declare function determineGrantType(authScheme: AuthScheme): OAuthGrantType | undefined;
export declare function exchangeClientCredentials({ authCredential, authScheme, }: {
    authCredential: AuthCredential;
    authScheme: AuthScheme;
}): Promise<ExchangeResult>;
export declare function exchangeAuthorizationCode({ authCredential, authScheme, }: {
    authCredential: AuthCredential;
    authScheme: AuthScheme;
}): Promise<ExchangeResult>;
