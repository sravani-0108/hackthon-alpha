import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
function escapeHtml(unsafe) {
  return unsafe.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#039;");
}
function formatSkillsAsXml(skills) {
  if (!skills || skills.length === 0) {
    return "<available_skills>\n</available_skills>";
  }
  const lines = ["<available_skills>"];
  for (const item of skills) {
    const frontmatter = "frontmatter" in item ? item.frontmatter : item;
    lines.push("  <skill>");
    lines.push("    <name>".concat(escapeHtml(frontmatter.name), "</name>"));
    lines.push(
      "    <description>".concat(escapeHtml(frontmatter.description), "</description>")
    );
    lines.push("  </skill>");
  }
  lines.push("</available_skills>");
  return lines.join("\n");
}
export {
  formatSkillsAsXml
};
